{
  "name": "umiyaji-dining-hall-railway",
  "version": "1.0.0",
  "description": "Umiya Ji Dining Hall Management System",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}